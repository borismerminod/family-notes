import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.familynotes.app',
  appName: 'Family Notes',
  webDir: 'dist/project_tmp/browser'
};

export default config;
